// Leere Huelle des Frontends. Ein generiertes Projekt haengt hier seine
// eigenen Feature-Module ein (Bildschirme, Verbindung zum Backend, Routing
// zwischen Modi) -- dieses Geruest trifft dazu bewusst keine Annahme.
export default function App() {
  return (
    <main>
      <h1>{{PROJECT_NAME}}</h1>
      {/* Feature-Module docken hier an. */}
    </main>
  );
}
